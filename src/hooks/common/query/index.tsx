import { useQuery } from "@tanstack/react-query";
import { Get } from "../../../utils/apiService";
import { apiUrl } from "../apiUrls";
import { keys } from "../keys";
const baseUrl = "http://localhost:5034/api/"; //process.env.REACT_APP_API_URL;

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////// Get All Task Types 
const getAllTaskTypes = async () => {
  return Get({
    url: `${baseUrl}${apiUrl.GET_TASK_TYPES}`,
  });
};
export const useGetAllTaskTypes = () => {
  return useQuery({
    queryKey: [keys.GET_ALLL_TASK_TYPES],
    queryFn: () => getAllTaskTypes(),
  });
};

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////// Get By ID Task Types 
export const getByIdTaskTypes = async (id: number) => {
  return Get({
    url: `${baseUrl}${apiUrl.GET_BY_ID_TASK_TYPES}/${id}`,
  });
};
export const useGetByIdTaskTypes = (id: number) => {
  return useQuery({
    queryKey: [keys.GET_ALLL_TASK_TYPES, id],
    queryFn: () => getByIdTaskTypes(id),
    enabled: id > 0,
  });
};

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////// Get All Priorities 
const getAllPriorities = async () => {
  return Get({
    url: `${baseUrl}${apiUrl.GET_ALLL_PRIORITIES}`,
  });
};
export const useGetAllPriorities = () => {
  return useQuery({
    queryKey: [keys.GET_ALLL_PRIORITIES],
    queryFn: () => getAllPriorities(),
  });
};

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////// Get By ID Priorities 
export const getByIdPriorities = async (id: number) => {
  return Get({
    url: `${baseUrl}${apiUrl.GET_BY_ID_PRIORITIES}/${id}`,
  });
};
export const useGetByIdPriorities = (id: number) => {
  return useQuery({
    queryKey: [keys.GET_BY_ID_PRIORITIES, id],
    queryFn: () => getByIdPriorities(id),
    enabled: id > 0,
  });
};

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////// Get All Status 
const getAllStatus = async () => {
  return Get({
    url: `${baseUrl}${apiUrl.GET_ALLL_STATUS}`,
  });
};
export const useGetAllStatus = () => {
  return useQuery({
    queryKey: [keys.GET_ALLL_STATUS],
    queryFn: () => getAllStatus(),
  });
};

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////// Get By ID Status 
export const getByIdStatus = async (id: number) => {
  return Get({
    url: `${baseUrl}${apiUrl.GET_BY_ID_STATUS}/${id}`,
  });
};
export const useGetByIdStatus = (id: number) => {
  return useQuery({
    queryKey: [keys.GET_BY_ID_STATUS, id],
    queryFn: () => getByIdStatus(id),
    enabled: id > 0,
  });
};

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////// Get All Projects 
const getAllProject = async () => {
  return Get({
    url: `${baseUrl}${apiUrl.GET_ALLL_PROJECTS}`,
  });
};
export const useGetAllProject = () => {
  return useQuery({
    queryKey: [keys.GET_ALLL_PROJECTS],
    queryFn: () => getAllProject(),
  });
};
